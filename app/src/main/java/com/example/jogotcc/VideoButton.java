package com.example.jogotcc;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.VideoView;

public class VideoButton extends VideoView {
    public VideoButton(Context context) {
        super(context);
        // Qualquer inicialização adicional que você possa precisar
    }

    public VideoButton(Context context, AttributeSet attrs) {
        super(context, attrs);
        // Qualquer inicialização adicional que você possa precisar
    }

    public VideoButton(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
        // Qualquer inicialização adicional que você possa precisar
    }
}
